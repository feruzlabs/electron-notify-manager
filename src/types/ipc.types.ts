export interface RepositionPayload {
  id: string;
  y: number;
}

export interface IpcNotificationConfig {
  id: string;
  title: string;
  description: string;
  image: string | null;
  duration: number;
}

