{
  "name": "electron-notify-manager-example",
  "private": true,
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.main.json && tsc -p tsconfig.renderer.json && node ./scripts/copy-renderer.mjs",
    "start": "npm run build && electron ."
  },
  "dependencies": {
    "electron-notify-manager": "file:.."
  },
  "devDependencies": {
    "electron": "^29.4.6"
  }
}

